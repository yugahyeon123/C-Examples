#include <stdio.h>
int main(void)		/*간단한 프로그램*/
{
    int num;		/*num이라는 변수를 정의한다.*/
    num = 1;		/*num에 값을 대입한다.*/
    printf("나는 단순한 ");		/*printf()함수를 사용한다.*/
    printf("컴퓨터 이다.\n");
    printf("나는 최고를 원하기 때문에 %d이라는 수를 좋아한다.\n", num);
    
    return 0;
}
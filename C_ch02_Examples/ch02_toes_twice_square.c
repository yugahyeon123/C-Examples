#include <stdio.h>
int main(void)
{
    int toes, twice, Square;
    toes = 10;

    twice = toes * 2;

    Square = toes * toes;

    printf("정수: %d\n",toes);
    printf("2배: %d\n",twice);
    printf("제곱: %d\n",Square);

}